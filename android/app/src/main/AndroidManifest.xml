<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ✅ Add this permission here, inside <manifest> but outside <application> -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <uses-permission android:name="android.permission.USE_FINGERPRINT"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Pushy SDK requirements -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Required on Android 13+ to display any notifications. flutter_local_notifications
         requests it at runtime via _initPushy() but the manifest declaration is
         still required so the runtime call succeeds. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- flutter_local_notifications adds SCHEDULE_EXACT_ALARM via manifest merge.
         We use inexactAllowWhileIdle only, so strip both exact-alarm permissions
         to keep Google Play Store policy clean. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" tools:node="remove" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" tools:node="remove" />

    <!-- Required on Android 14+ for fullScreenIntent: true to actually
         display a heads-up notification over the lockscreen / DND. We
         set this on the T-30m appointment reminder so the user is more
         likely to see it even if their phone is on silent / DND. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <application
        android:label="DocSera"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyChto7CVopmZurZamIsuLeIPwDts3o83Is" />


        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
                />

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- ✅ Deep Link: docsera://doctor/<doctorId> -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="docsera"/>
            </intent-filter>

            <!-- ✅ App Link: https://docsera.app/doctor/<token>
                 autoVerify="true" tells Android to attempt site association
                 via https://docsera.app/.well-known/assetlinks.json. Until
                 that file is published, tapping the URL shows a disambiguation
                 chooser (browser vs DocSera). Once assetlinks.json is live the
                 URL opens this app silently. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="docsera.app"
                    android:pathPrefix="/doctor/"/>
            </intent-filter>

        </activity>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2"
            />

        <!-- ===================================================== -->
        <!-- Pushy SDK services + receivers (per pushy_flutter      -->
        <!-- 2.0.43 canonical example manifest). Class names differ -->
        <!-- from the bare-Pushy-Android docs; this is the Flutter  -->
        <!-- variant: PushyInternalReceiver lives in the plugin's   -->
        <!-- me.pushy.sdk.flutter.internal package, NOT the SDK's   -->
        <!-- me.pushy.sdk.receivers package.                        -->
        <!-- ===================================================== -->

        <!-- Notification icon. Without this, Android shows a       -->
        <!-- generic placeholder. Android tints notification icons  -->
        <!-- to the system's notification color, so for a polished  -->
        <!-- look we should ship a proper monochrome white-on-      -->
        <!-- transparent ic_notify in res/drawable/. Until that's   -->
        <!-- done, point at ic_launcher_foreground (the bare logo   -->
        <!-- silhouette without the colored background tile).       -->
        <meta-data
            android:name="me.pushy.sdk.notification.icon"
            android:resource="@drawable/ic_notify" />

        <!-- Internal notification receiver — bridges the MQTT push -->
        <!-- to the Flutter listener registered by Pushy.listen().  -->
        <receiver
            android:name="me.pushy.sdk.flutter.internal.PushyInternalReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="pushy.me" />
            </intent-filter>
        </receiver>

        <!-- Restart the listener service after the app updates.    -->
        <receiver
            android:name="me.pushy.sdk.receivers.PushyUpdateReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Restart the listener service after device reboot.      -->
        <receiver
            android:name="me.pushy.sdk.receivers.PushyBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- MQTT socket service. -->
        <service android:name="me.pushy.sdk.services.PushySocketService" />

        <!-- Job service (Pushy 1.0.35+). exported=true is required  -->
        <!-- by the SDK's JobScheduler logic.                        -->
        <service
            android:name="me.pushy.sdk.services.PushyJobService"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="true" />
    </application>

    <!-- ✅ Update the <queries> section for URL Launcher -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

</manifest>
